
'use client';

// This component is now disabled to prevent the automatic pop-up.
// The music context and hooks remain functional if manual controls are added elsewhere.
export default function MusicPlayer() {
  return null;
}
