
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // WARNING: These rules are completely public and are NOT secure.
      // This is for development purposes only to eliminate permission errors.
      // You MUST secure your database before launching your app.
      allow read, write: if true;
    }
  }
}
